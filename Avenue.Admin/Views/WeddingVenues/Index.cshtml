﻿
@model IEnumerable<Avenue.DTO.WeddingVenuesDto>

@{
    ViewBag.Title = "لیست";
}

<h3>لیست سرویسها
</h3>
    <div class="container-fluid">
   <!-- START DATATABLE 1 -->
   <div class="row">
      <div class="col-lg-12">
         <div class="panel panel-default">
            <div class="panel-heading">
                
               <small>@Html.ActionLink("افزودن", "Create", null, new { @class = "mb-sm btn btn-primary"})</small>
            </div>
            <div class="panel-body">
                <div class="table-responsive">
                   <table id="datatable1" class="table table-striped table-hover">
   <thead>
    <tr>
 
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
     
  
        <th>
            @Html.DisplayNameFor(model => model.Tel)
        </th>
   
 

        <th></th>
    </tr>
	</thead>
                            <tbody>

@foreach (var item in Model) {
    <tr>
 
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
      
        <td>
            @Html.DisplayFor(modelItem => item.Tel)
        </td>
      
        <td>
            @Html.ActionLink(" ", "Edit", new { id = item.Id }, new { @class = "btn btn-primary  btn-sm  fa fa-pencil-square-o", @title = "", @data_toggle = "tooltip", @data_title = " ویرایش" })
            @Html.ActionLink(" ", "Delete", new { id = item.Id }, new { @class = "btn btn-danger  btn-sm  fa fa-trash-o delete", @title = "", @data_toggle = "tooltip", @data_title = "حذف" })
            @Html.ActionLink(" ", "PhotoMangment", new { id = item.Id }, new { @class = "btn btn-danger  btn-sm  fa fa-file-image-o delete", @title = "", @data_toggle = "tooltip", @data_title = "مدیریت تصاویر" })
            @*@if(item.CategoryId == ) { }*@
            @Html.ActionLink("افزودن منو", "MenuItems", new { id = item.Id }, new { @class = "btn btn-success", @title = "افزودن منو", @data_toggle = "tooltip", @data_title = "افزودن منو" })
        </td>
    </tr>
}
 </tbody>
                   </table>
                </div>
            </div>
         </div>
      </div>
   </div>
   <!-- END DATATABLE 1 -->


</div>


@section Styles {
    @Styles.Render("~/bundles/datatablesCss")
}
@section Scripts {
    @Scripts.Render("~/bundles/datatables")
    @Scripts.Render("~/bundles/demoDatatable")
}

  